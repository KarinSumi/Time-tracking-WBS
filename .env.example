# Database connection string (SQLite file path)
DATABASE_URL="file:./prisma/dev.db"

# Secret key used for signing JWT tokens (must be replaced with a secure random key in production)
JWT_SECRET="aion-dev-secret-key-change-in-production"

# Application server port
PORT=5050

# Node Environment Mode (development / production)
NODE_ENV="development"
